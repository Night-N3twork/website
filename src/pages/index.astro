---
// English root route. SSR-rendered so middleware can do Accept-Language
// redirects to localized prefixes.
export const prerender = false;
import Home from '@components/pages/Home.astro';
---

<Home />
