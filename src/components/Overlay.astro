---
export interface Props {
	enableBg?: boolean;
	customClasses?: string;
}

const { enableBg = true, customClasses = '' } = Astro.props;

const bgClass = enableBg
	? 'bg-night-mantle backdrop-blur-md border border-night-base shadow-lg'
	: '';
---

<div
	class={`text-night-text rounded-xl p-4 md:p-6 ${bgClass} ${customClasses}`}
>
	<slot />
</div>
